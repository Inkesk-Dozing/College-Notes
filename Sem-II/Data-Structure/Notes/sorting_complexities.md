# Complexities

Quick sort worst case time complexity is O(n^2):

Q: Write worst case for quick sort.
    that is when we take pivot as the end element

always take pivot at best position that is middle position.

Avg case O(log(n))
    we use stack to make it so and search.

worst space O(n)
    when implementing worst case the stack is completely full
    that is we take it 'n' times

and its stable.

i in crement complete j and pivot if j is less than pivot then sawp if j is moe hta pivot no swap 
if i less tha pivot do not change 